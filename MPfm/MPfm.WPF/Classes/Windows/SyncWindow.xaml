<base:BaseWindow x:Class="MPfm.WPF.Classes.Windows.SyncWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"        
        xmlns:base="clr-namespace:MPfm.WPF.Classes.Windows.Base"
        xmlns:controls="clr-namespace:MPfm.WPF.Classes.Controls"
        xmlns:models="clr-namespace:MPfm.Library.Objects;assembly=MPfm.Library"
        WindowStartupLocation="CenterScreen" Background="#FF242F35"
        Title="Sync (Nearby Devices)" Height="426" Width="677.9" Icon="/MPfm.WPF;component/Resources/Icon.ico">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#FF455865">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Sync (Nearby Devices)" Foreground="White" FontSize="14" FontFamily="/Resources/Fonts/#Roboto" />
            <Label Grid.Column="1" x:Name="lblIPAddress" Content="" Foreground="#FFDDDDDD" FontSize="12" FontFamily="/Resources/Fonts/#Roboto" VerticalContentAlignment="Center" />
        </Grid>        
        <Grid Grid.Row="1" Margin="4,8,4,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="100" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Foreground="#FFDDDDDD" FontSize="11" FontFamily="/Resources/Fonts/#Roboto">
                Searching for devices running Sessions on your local network...
            </TextBlock>
            <ProgressBar Grid.Column="1" x:Name="progressBar" Background="#FF242F35" BorderBrush="#FF242F35" Foreground="#FFE74C3C" Value="0" Margin="2" VerticalContentAlignment="Center" />
        </Grid>
        <ListView Grid.Row="2" x:Name="listView">
            <ListView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type models:SyncDevice}" ItemsSource="{Binding SubItems}">
                    <StackPanel Orientation="Horizontal" Margin="4">
                        <Image Source="/Resources/Images/Icons/linux.png" Stretch="Uniform" />
                        <TextBlock Text="{Binding Name}" FontSize="11" FontFamily="/Resources/Fonts/#Junction" Foreground="#333333" Margin="4,0,0,0" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </ListView.Resources>
        </ListView>
        <Grid Grid.Row="3" Margin="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <controls:ImageButton Grid.Column="0" x:Name="btnRefresh" Title="Cancel refresh" ImageSource="/Resources/Images/Buttons/cancel.png" Style="{StaticResource ImageButton}" Click="btnRefresh_OnClick" />
            <StackPanel Grid.Column="1"></StackPanel>
            <controls:ImageButton Grid.Column="2" x:Name="btnConnectManual" Title="Connect to device" ImageSource="/Resources/Images/Buttons/connect.png" Style="{StaticResource ImageButton}" Click="btnConnectManual_OnClick" />
            <controls:ImageButton Grid.Column="3" x:Name="btnConnect" Title="Connect to device" ImageSource="/Resources/Images/Buttons/connect.png" Style="{StaticResource ImageButton}" />
        </Grid>        
    </Grid>
</base:BaseWindow>

    